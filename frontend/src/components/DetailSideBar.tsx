import { useEffect } from "react";

const DetailSideBar = ({ isOpen, onClose }: any) => {

  useEffect(()=> {
    if(isOpen){
        
    }
  })

  onClose();
  return (
    <div>
      {/* Sidebar content goes here */}
    </div>
  );
};

export default DetailSideBar
